import { View, Text } from "react-native";
import React from "react";
import SafeAreaWrapper from "@/src/components/SafeAreaWrapper";

const CreateRoutine = () => {
  return <SafeAreaWrapper></SafeAreaWrapper>;
};

export default CreateRoutine;
